function projectPreparation(name, project) {
    let hours = project * 3;

    console.log(`The architect ${name} will need ${hours} hours to complete ${project} project/s.`);
}

projectPreparation("Sanya", 9)